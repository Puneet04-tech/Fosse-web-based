# Desktop Frontend — Fosse Chemical Equipment Visualizer

Quick start:

1. Create a Python virtualenv and activate it.
2. pip install -r requirements.txt
3. (Optional) set environment variable API_URL to point to your backend, e.g. `set API_URL=http://127.0.0.1:8000/api`
4. python app.py

Notes:

- Upload requires Basic Auth credentials if the backend enforces it.
